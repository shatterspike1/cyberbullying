#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../wintersmith/bin/wintersmith" "$@"
  ret=$?
else 
  node  "$basedir/../wintersmith/bin/wintersmith" "$@"
  ret=$?
fi
exit $ret
